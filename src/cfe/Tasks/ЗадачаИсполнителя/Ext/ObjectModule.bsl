
&После("ПередЗаписью")
Процедура ЦППК_ПередЗаписью(Отказ)
	
	Если ЭтотОбъект.ТочкаМаршрута = ПредопределенноеЗначение("БизнесПроцесс.Исполнение.ТочкаМаршрута.Проверить") Тогда
	
		ЭтотОбъект.СрокИсполнения = Дата('00010101');	
	
	КонецЕсли;

КонецПроцедуры
