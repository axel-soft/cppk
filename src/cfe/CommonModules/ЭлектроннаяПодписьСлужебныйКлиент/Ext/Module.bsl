
&ИзменениеИКонтроль("ВыполнитьНаСторонеЦиклПослеЗаписиПодписи")
Процедура ЦППК_ВыполнитьНаСторонеЦиклПослеЗаписиПодписи(Результат, Контекст)

	Если Результат.Свойство("ОписаниеОшибки") Тогда
		Контекст.ЭлементДанных.Удалить("СвойстваПодписи");
		Ошибка = Новый Структура("ОписаниеОшибки",
		НСтр("ru = 'При записи подписи возникла ошибка:'") + Символы.ПС + Результат.ОписаниеОшибки);
		ВыполнитьНаСторонеПослеЦикла(Ошибка, Контекст);
		Возврат;
	КонецЕсли;
	
	#Вставка
	
	//#Если Не ВебКлиент И Не МобильныйКлиент Тогда
	//Если НЕ Результат.Свойство("ОписаниеОшибки") Тогда
	//		
	//	ДанныеФайла = РаботаСФайламиВызовСервера.ДанныеФайлаДляОткрытия(
	//		Контекст.ЭлементДанных.Представление, 
	//		Неопределено, 
	//		Контекст.ИдентификаторФормы);
	//	
	//	Если ДанныеФайла.ТребуетсяСоздатьВизуализациюЭП
	//		И ДанныеФайла.ВизуализацияЭПДобавлена = Ложь Тогда 
	//			
	//			// для doc файла заполним на клиенте - если не веб клиент
	//			РаботаСФайламиКлиент.СоздатьВизуализациюЭПВФайлеDocXls(ДанныеФайла, Контекст.ИдентификаторФормы);
	//			
	//			
	//	КонецЕсли;
	//		
	//КонецЕсли;
	//#КонецЕсли
	
	#КонецВставки

	ВыполнитьНаСторонеЦиклНачало(Контекст);

КонецПроцедуры
