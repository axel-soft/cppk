
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ПоказатьВопрос(Новый ОписаниеОповещения("ОбработкаКомандыПослеВопроса", ЭтотОбъект),
		НСтр("ru = 'Вернуться к схеме данных из конфигурации?'"),
		РежимДиалогаВопрос.ДаНет);
		
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаКомандыПослеВопроса(Результат, Параметры) Экспорт
	
	Если Результат = КодВозвратаДиалога.Да Тогда
		ОчиститьСхемуДанныхКОД();
		Оповестить("КОД_ИзмененаСхемаДанных");
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ОчиститьСхемуДанныхКОД()
	
	НачатьТранзакцию();
	Константы.ДатаЗагрузкиСхемыДанныхКОД.Установить(Дата(1, 1, 1));
	Константы.ХранилищеСхемыДанныхКОД.Установить(Новый ХранилищеЗначения(Неопределено));
	ЗафиксироватьТранзакцию();
	ОбновитьПовторноИспользуемыеЗначения();
	
КонецПроцедуры