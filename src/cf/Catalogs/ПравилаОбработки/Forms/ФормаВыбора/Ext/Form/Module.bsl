#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.ТолькоВидыОбъектов Тогда 
		Список.Параметры.УстановитьЗначениеПараметра("ТолькоВидыОбъектов", Истина);
	КонецЕсли;
	
	Если Параметры.Свойство("ВидОбъекта") И ЗначениеЗаполнено(Параметры.ВидОбъекта) Тогда
		Запрос = Новый Запрос();
		Запрос.Текст = 
			"ВЫБРАТЬ
			|	СправочникПравилаОбработки.Ссылка КАК Правило
			|ИЗ
			|	Справочник.ПравилаОбработки КАК СправочникПравилаОбработки
			|ГДЕ
			|	СправочникПравилаОбработки.ЭлементГруппировки = &ВидОбъекта";
		Запрос.Параметры.Вставить("ВидОбъекта", Параметры.ВидОбъекта);
		
		Список.Параметры.УстановитьЗначениеПараметра("КорневыеПравила", 
			Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Правило"));	
	КонецЕсли;	
	
КонецПроцедуры

#КонецОбласти