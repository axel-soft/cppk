#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда
		Возврат;
	КонецЕсли;
	
	Если Параметры.Свойство("Отбор")
			И Параметры.Отбор.Свойство("Узел")
			И ЗначениеЗаполнено(Параметры.Отбор.Узел) Тогда
		Элементы.Узел.Видимость = Ложь;
	КонецЕсли;
	
	Если Параметры.Свойство("Отбор")
			И Параметры.Отбор.Свойство("ИдентификаторКлюча")
			И ЗначениеЗаполнено(Параметры.Отбор.ИдентификаторКлюча) Тогда
		Элементы.ИдентификаторКлюча.Видимость = Ложь;
	КонецЕсли;
	
	Если Параметры.Свойство("Отбор")
			И Параметры.Отбор.Свойство("ТипКлюча")
			И ЗначениеЗаполнено(Параметры.Отбор.ТипКлюча) Тогда
		Элементы.ТипКлюча.Видимость = Ложь;
	КонецЕсли;
	
	Если Параметры.Свойство("Отбор")
			И Параметры.Отбор.Свойство("Объект")
			И ЗначениеЗаполнено(Параметры.Отбор.Объект) Тогда
		Элементы.Объект.Видимость = Ложь;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти