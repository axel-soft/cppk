
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ВидКлюча = ОтметкиВремениПовтИсп.ВидКлючаОбъекта(Запись.Объект);
	Если ОтметкиВремениПовтИсп.ЭтоВидКлючаСсылочногоТипа(ВидКлюча) Тогда
		ПредставлениеКлюча =
			ОтметкиВремени.ЗначениеСсылкиПоКлючуОтметкиВремени(Запись.ИдентификаторКлюча, Запись.ТипКлюча);
	Иначе
		ПредставлениеКлюча = Запись.ИдентификаторКлюча;
	КонецЕсли;
	
	МенеджерЗаписи = РегистрыСведений.НепринятыеВерсииДанных.СоздатьМенеджерЗаписи();
	МенеджерЗаписи.ИдентификаторКлюча = Запись.ИдентификаторКлюча;
	МенеджерЗаписи.ТипКлюча = Запись.ТипКлюча;
	МенеджерЗаписи.Объект = Запись.Объект;
	МенеджерЗаписи.Узел = Запись.Узел;
	МенеджерЗаписи.ИмяСообщения = Запись.ИмяСообщения;
	МенеджерЗаписи.Прочитать();
	
	СтрокаXML = МенеджерЗаписи.ДанныеВерсии.Получить();
	
КонецПроцедуры

#КонецОбласти
