
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Запрос = Новый Запрос(
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	ДанныеДляОтложеннойОбработки.Данные КАК Данные
		|ИЗ
		|	РегистрСведений.ДанныеДляОтложеннойОбработки КАК ДанныеДляОтложеннойОбработки
		|ГДЕ
		|	ДанныеДляОтложеннойОбработки.Идентификатор = &Идентификатор");
	
	Запрос.УстановитьПараметр("Идентификатор", Запись.Идентификатор);
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		ПараметрыСтрокой = ЗначениеВСтрокуВнутр(Выборка.Данные.Получить());
	КонецЕсли;
	
КонецПроцедуры
